---
doc_id: rag-overview-03
doc_type: overview
project: zhengtu-server
module: global
source_path: ztgame/start.sh,ztgame/start1.sh,ztgame/stop.sh
tags: ['runtime','startup','rag']
confidence: high
last_verified: 2026-04-30
---
# 03_服务端启动顺序（v0.3）

## 1) 仓库 `start.sh` 顺序（事实）
`SuperServer -> RecordServer -> BillServer -> MiniServer -> SessionServer -> ScenesServer -> GatewayServer`。
- `FLServer` 在该脚本中被注释。

## 2) 仓库 `start1.sh` 顺序（事实）
`SuperServer -> FLServer -> RecordServer -> BillServer -> MiniServer -> SessionServer -> ScenesServer -> GatewayServer`。

## 3) 当前源码机实测/推荐验证顺序
`FLServer -> UserServer -> SuperServer -> RecordServer -> BillServer -> MiniServer -> SessionServer -> ScenesServer -> GatewayServer`。

- 说明：仓库内未发现 `UserServer/` 目录；`UserServer` 可能来自部署目录或历史组件，需人工确认。
- 若当前环境无 `UserServer`，可按 `FLServer -> SuperServer -> RecordServer -> BillServer -> MiniServer -> SessionServer -> ScenesServer -> GatewayServer` 回退验证。

## 4) `stop.sh` 停止逻辑
- 通过 `ps aux | grep ./<Server> | awk '{print $2}'` 获取 PID。
- 使用 `kill -9` 强制停止：`SuperServer`、`FLServer`、`roleRegServer`、`InfoServer`。
- 风险：强制 kill 可能导致日志未落盘、会话未清理（待确认）。

## 5) 启动失败优先检查项
1. 二进制是否存在且可执行（例如 `ScenesServer/ScenesServer`）。
2. 基础静态库是否完整（`libzebra.a`、`libencdec.a`、`libcrypto.a`、`liblua.a`、`libluabind.a`）。
3. 配置文件路径与权限（`Config/*.xml`、根目录 `*.xml`、`quest/`、`newquest/`）。
4. 数据库连通性（`mysql_config` 相关依赖、连接参数来源待确认）。
5. 日志目录与权限（脚本会清理 `/tmp/*.log*`）。
6. 启动顺序是否满足上游服务先启动。
