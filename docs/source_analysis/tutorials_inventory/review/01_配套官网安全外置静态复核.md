# 配套官网安全外置静态复核（静态）

## 目标文件检查
- `配套官网/.env.example`：**不存在**。
- `配套官网/db.php`：存在，但**仍包含硬编码敏感值**，未完成“全量环境变量外置”。
- `配套官网/scripts/epay_sign.php`：**不存在**。
- `配套官网/tests/payment_notify_cases.md`：**不存在**。

## db.php 复核结论
1. 是否使用 `env_get`：**否**（未见统一 env 读取函数）。
2. 是否移除硬编码真实 DB/SMTP/EPAY：**否**。
   - DB 主机、账号、口令为明文；
   - SMTP 账号/授权码为明文；
   - EPAY_KEY/EPAY_PID/通知与返回 URL 为明文。
3. 常量兼容性：**是，保留了兼容常量定义**。
   - `DB_DSN/DB_USER/DB_PWD/RECHARGE_RATE/EPAY_*` 仍有 `define`。

## epay_sign 与 payment_notify_cases
- 由于文件不存在，无法确认“仅本地签名辅助”“仅回归说明”属性。
- 建议补齐对应文件并改为占位模板内容（无真实 key/pid/domain）。

## 结论
- 本轮复核显示：此前宣称的“官网安全外置改造”在当前仓库快照中**未达标**。
- 优先级建议：提升为 P0 安全治理子线（至少先完成 `.env.example + db.php` 去敏）。
