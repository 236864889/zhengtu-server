# 00_教程目录总览

## 扫描范围
- 根目录按任务指定条件扫描：`*教程*/*安装*/*编译*/*部署*/*启动*/*说明*/*README*/*.md/*.txt/*.doc/*.docx/*.sh`（`maxdepth 5`）。
- 重点目录追加扫描：`docs/`、`教程/`、`tutorials/`、`ztgame/`。
- 关键词检索范围：`docs ztgame 教程 tutorials .`，并按要求仅保留教程/说明类信息用于本报告。

## 发现的教程目录
1. `docs/rag_sources/`（结构化编译/启动/配置文档）
2. `docs/source_analysis/`（历史源码分析与流程文档）
3. `教程系列_上传版/服务端说明/`
4. `教程系列_上传版/客户端说明/`
5. `教程系列_上传版/网络收集教程/`
6. `ztgame/` 各服务子目录 README（Bill/FL/Gateway/Mini/Record/Scenes/Session/Super/base/script 等）
7. `ztgame.dat/` 与 `登录器/` 下客户端相关说明文档

> `教程/`、`tutorials/` 目录扫描结果为空（仓库中未发现这两个目录的有效文档文件）。

## 发现的教程文件（汇总）
- 候选文档总数：**550**（`/tmp/tutorial_candidate_files.txt`）。
- 其中教程密集区：
  - `docs/`：255
  - `教程系列_上传版/`：231
  - `ztgame.dat/`：30
  - `ztgame/`：23

## 教程文件类型统计
- `.md`：282
- `.txt`：245
- 无后缀：10
- `.doc`：6
- `.sh`：3
- `.rtf`：2
- `.html`：1
- `.hlp`：1

## 是否存在重复教程
结论：**存在明显重复**。
- 同主题在多个目录重复：例如 NPC/脚本变量、征途技能修改、装备修改、queston_visit 说明在 `服务端说明` 与 `网络收集教程/教程` 中重复出现。
- 文件名高重复：如“征途NPC出售物品.txt”“如何加绝世的装备.txt”“技能修改.txt”等跨目录多份。

## 是否存在明显过期教程
结论：**存在大量过期/高风险旧教程**。
- 明显旧环境：CentOS 6.5、Win1.1、ASP 注册站、老版 GM 工具、私服向导类文档。
- 存在不适配当前仓库目标的内容：破解引擎、历史外网映射、旧版二进制补丁文档。
- 与当前阶段冲突：RAG 入库执行说明仍在 docs 中，但当前任务已明确暂停。

## 是否存在重要缺口
结论：**存在“可追溯、可执行、单一权威手册”的缺口**。
- 缺少一份统一的“从环境准备到联调验收”的正式手册。
- 缺少对“当前可用流程”和“历史资料”边界的明确标注。
- 缺少统一版本基线（例如：CentOS6.5 历史链路 vs 当前主机可替代方案）。
