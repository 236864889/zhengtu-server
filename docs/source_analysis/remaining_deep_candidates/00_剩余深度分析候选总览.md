# 剩余深度分析候选总览

## 扫描范围
- 仓库根目录（`find . -maxdepth 2 -type d`）。
- `ztgame` 目录两层子目录（`find ztgame -maxdepth 2 -type d`）。
- 源码与文档文件集合（`find . -maxdepth 4 -type f` + C/C++/Lua/XML/SQL/SH/MD/TXT 过滤）。
- 既有分析文档覆盖面：`docs/source_analysis` 与 `docs/operations`。

## 已覆盖方向（本轮不重复深挖）
- 已有模块 deep：`base`、`SessionServer`、`GatewayServer`、`ScenesServer`、`RecordServer`、`BillServer`、`SuperServer`、`FLServer`、`MiniServer`、`Config`、`script`、`newquest`、`quest`、`tools`。
- 客户端/登录器/教程/操作手册方向已有完整阶段性文档。

## 未覆盖目录（本轮新增候选）
- `ztgame/test`：存在，含多类基础能力测试样例（网络、线程、MySQL、配置解析、日志）。
- `ztgame/myflies`：存在，疑似历史 Visual Studio 工程残留（`.sln`/`.vcproj`）。

## 重点路径存在性检查结果
- 未发现：`ztgame/UserServer`、`ztgame/DBAccess`、`ztgame/dbaccess`、`ztgame/GMServer`、`ztgame/InfoServer`、`ztgame/roleRegServer`、`ztgame/tests`、`ztgame/sql`、`ztgame/SQL`、`ztgame/data`。
- 已发现：`ztgame/test`、`ztgame/myflies`。

## 候选目录优先级
1. **P1：`ztgame/test` 深度分析**
   - 价值：可补齐“公共基础库验证方法/样例输入输出/协议与线程边界”的源码证据链。
   - 产出建议：测试样例索引、可复用最小调用链、历史调试入口清单。
2. **P2：顶层启动脚本与入口配置链路（仓库根 + `ztgame/tools` 关联）**
   - 现状：仓库根未发现 `.sh/.xml/.sql`；需转向 `ztgame/tools` 与子目录脚本入口做“入口级补缺”。
3. **P3：`ztgame/myflies` 历史残留审计**
   - 价值：低；主要用于确认可忽略边界与文档化归档策略。

## 不建议继续目录
- 缺失目录（如 `UserServer/DBAccess/GMServer/...`）当前不建议投入 deep；应先在历史文档或外部包确认是否另仓或已裁剪。
- `myflies` 不建议高投入深挖源码（几乎无业务源码，仅工程壳）。

## 下一轮建议
- 第一优先：`ztgame/test`（补齐服务端基础设施“验证样例视角”的 deep 文档）。
- 第二优先：脚本入口链（重点查 `ztgame/tools` 内启动/部署/发布脚本与 XML/SQL 关联）。
- 第三优先：整理“缺失核心目录”追踪单，标注来源线索与待补仓路径。
