# 00 配套官网去敏修订报告

## 本轮改造内容
- 新增 `.env.example`，仅保留占位值。
- `db.php` 通过 `env_get` 读取 DB/SMTP/EPAY/站点配置，并保留旧常量兼容。
- 清理 `balance_query.php`、`cdk_recharge.php`、`register_compat.php`、`lottery.php` 中硬编码 DB 默认敏感值。
- `lottery.php` 的抽奖接口地址改为相对路径 `./lottery_draw.php`，避免硬编码公网 IP。
- 新增 `README_SECURITY.md` 安全说明。

## 风险状态
- 已移除业务代码中的明文 DB 密码与公网 IP 默认回退值。
- 仍需人工确认历史文档/页面文案中的外部链接是否保留。
