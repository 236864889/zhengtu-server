# 结论一致性检查

## 检查项
1. `start.sh/start1.sh` 表述一致性：
   - 结论：一致标记为“顺序启动脚本，含高副作用，需 dry-run 改造”。
2. UserServer 标记：
   - 结论：均应标记“待确认/仓库缺失目录”，不得写成已确认部署必需。
3. Codex/Claw 边界：
   - 结论：Codex 做静态与脚本改造设计；Claw/服务器团队做实机只读检查。
4. test/tools 不直接运行约束：
   - 结论：一致，所有 deep 文档均不应给出实测结果。
5. RAG 入库后置：
   - 结论：一致，先完成索引与一致性，再执行正式入库。

## 本轮核对证据
- `test_deep/09`、`tools_entry_deep/08`、`remaining_deep_candidates/04`。
- `ztgame/start.sh`、`ztgame/start1.sh`、`ztgame/stop.sh` 静态文本。
