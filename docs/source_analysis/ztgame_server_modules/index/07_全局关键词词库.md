# 全局关键词词库

## 服务进程
SuperServer, GatewayServer, SessionServer, ScenesServer, RecordServer, BillServer, MiniServer, FLServer, serviceCallback, init, main。

## 启动脚本
start.sh, start1.sh, stop.sh, kill -9, 顺序启动, 停服策略, 进程编排。

## 配置/XML
ServerList, gameconfig.xml, map xml, 配置漂移, 缺失字段, 环境基线。

## Lua/Quest
luabind, execute_script_event, quest, newquest, NPC 脚本, 任务奖励。

## 数据库
MySQL, 写库风险, 账变一致性, 事务边界, SQL 工具。

## 网络协议
登录协议, Session 链路, Gateway 转发, tempid, 封包加密。

## 登录器/客户端
launcher, patch, ztgame.dat, config.ini, 客户端服务端映射。

## test/tools
testGame, skill_test_client, zebraclientService, zntop, datetime, base64。

## 风险
高副作用, 强杀, 敏感配置, 配置缺失, 兼容性风险。

## dry-run
no-op, 只读检查, plan 模式, preflight, rollback。

## RAG
manifest, collection, chunk_hint, ingest_ready, 检索路由。
