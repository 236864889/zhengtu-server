# 07_v1_1发布前复核报告

## 检查时间与范围
- 检查时间：2026-05-01（UTC）。
- 检查范围：
  - `docs/operations/征途源码编译部署完整操作手册.md`
  - `docs/operations/附录A_历史教程映射清单.md`
  - `docs/operations/附录B_常见错误速查表.md`
  - `docs/operations/附录C_Codex_Claw任务模板.md`
  - `docs/operations/review/06_v1_1修订记录.md`
  - `ztgame/Makefile`
  - `ztgame/start.sh`
  - `ztgame/start1.sh`
  - `ztgame/stop.sh`
- 执行方式：只读复核；未编译、未启动、未执行二进制。

## 检查项逐项结果
1. 主手册存在：**通过**。
2. 主手册标题包含 v1.1：**通过**（标题为“征途源码编译部署完整操作手册 v1.1”）。
3. 附录A存在：**通过**。
4. 附录B存在：**通过**。
5. 附录C存在：**通过**。
6. review/06 存在：**通过**。
7. “来源路径可用性检查”存在：**通过**（主手册 §0）。
8. `SUB_DIRS` 顺序存在并明确：**通过**（主手册写明以 `ztgame/Makefile` 为准，且顺序与 Makefile 一致）。
9. `script` 非顶层但链接依赖说明存在：**通过**（主手册写明 `script` 不在顶层 `SUB_DIRS`，但 `ScenesServer` 链接 Lua/luabind 静态库）。
10. 启动双基线（`start.sh`/`start1.sh`）并列说明：**通过**。
11. UserServer 待确认说明存在：**通过**（主手册专章说明“必须人工确认”）。
12. `stop.sh` `kill -9` 风险说明存在：**通过**。
13. 历史教程 C/D 级降级说明存在：**通过**（附录A给出 C 级仅历史参考、D 级不映射并排除）。
14. Codex/Claw 模板完整：**通过**（附录C含 8 类任务模板）。

## 缺失项
- 本轮复核未发现结构性缺失项。

## 风险项
1. `ztgame/start.sh` 与 `ztgame/stop.sh` 首行 shebang 异常（`!/bin/bash`、`!#/bin/bash`），存在脚本可执行兼容风险；主手册已采用“事实基线”写法，但上线前仍需人工确认脚本执行方式。
2. `stop.sh` 采用 `kill -9`，存在误杀与未落盘风险；文档已标注风险，但实际生产流程仍需更稳妥停服策略。
3. UserServer 角色仍未落地，属于上线 P0 决策项。

## 建议修订项
1. 建议在 v1.2 中补充“脚本 shebang 异常的执行兼容说明”（如必须显式 `bash start.sh`）。
2. 建议在 v1.2 中补充“停服分级策略”（优雅停服超时后再强停）。
3. 建议在 v1.2 中把 UserServer 结论（需要/不需要/外部组件）固化为发布版条目。

## 是否需要 v1.2
- 结论：**建议需要 v1.2（小版本）**。
- 原因：v1.1 结构已可用，但仍有上线前 P0 信息待确认，且脚本执行与停服策略可进一步标准化。
