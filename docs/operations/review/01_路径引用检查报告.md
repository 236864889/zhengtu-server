# 路径引用检查报告

## 检查对象
- 手册目标路径（v1.0）：`docs/operations/*.md`
- 关键引用路径：
  - `docs/rag_sources/`
  - `ztgame/start.sh`
  - `ztgame/start1.sh`
  - `ztgame/stop.sh`
  - `docs/source_analysis/ztgame_dat/runtime_validation/`
  - `docs/source_analysis/launcher_reanalysis/`
  - `教程系列_上传版/`

## 存在路径
- `docs/rag_sources/`（存在，且包含 00~12 系列文档）。
- `ztgame/start.sh`（存在）。
- `ztgame/start1.sh`（存在）。
- `ztgame/stop.sh`（存在）。
- `docs/source_analysis/ztgame_dat/runtime_validation/`（存在）。
- `docs/source_analysis/launcher_reanalysis/`（存在）。

## 缺失路径
- `docs/operations/征途源码编译部署完整操作手册.md`（缺失）。
- `docs/operations/附录A_历史教程映射清单.md`（缺失）。
- `docs/operations/附录B_常见错误速查表.md`（缺失）。
- `docs/operations/附录C_Codex_Claw任务模板.md`（缺失）。
- `教程系列_上传版/`（当前仓库未检出该目录）。

## 待确认路径
- 若 v1.0 文档实际位于其他分支/提交，需确认是否尚未合入当前分支。
- 若 `教程系列_上传版/` 为外部挂载目录，需确认是否在本仓库约定范围内。

## 建议修复
1. 在 `docs/operations/` 下补建主手册及 3 个附录。
2. 对所有外部历史资料路径增加“可选来源/可能缺失”标注，避免写成硬依赖。
3. 在手册内新增“来源可用性检查”小节，先校验路径再执行步骤。
